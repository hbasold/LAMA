node Switch(on, off: bool) returns (so: bool);
let
  state
    s : bool;
  local
    s_ : bool;
  definition
    s_ = (ite s (not off) on);
  output
    so = s_;
  transition
    s' = s_;
  initial s = false;
tel
