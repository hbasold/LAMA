nodes
  node UpDown() returns (x : int);
  let
    state
      s : int;
      up : bool;
    local
      s_ : int;
    definition
      s_ = (ite up (+ s 1) (- s 1)) ;
    transition
      s' = s_;
      up' = (ite up (< s_ 10) (= s_ 0));
    output
      x = s_;
    initial s = (- 1), up = true;
  tel
local x : int;
definition x = (use UpDown);
