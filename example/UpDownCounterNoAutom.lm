nodes
  node UpDown() returns (x : int);
  let
    local
      s_ : int;
    state
      s : int;
      up : bool;
    definition
      s_ = (ite up (+ s 1) (- s 1)) ;
    transition
      s' = s_;
      up' = (ite up (< s_ 10) (= s_ 0));
    output
      x = s_;
    initial s = (- 1), up = true;
  tel
local x : int;
definition x = (use UpDown);
