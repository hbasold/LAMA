node Switch(on, off : bool)
returns (s : bool)
var s_1 : bool;
let
   s_1 = false -> pre s;
   s = if s_1 then not off else on;
tel

/* Properties:
always(on implies (next s));
always(off implies (next not s));
always((not off and not on) implies (exists ls = s: next (s = ls)));
*/
